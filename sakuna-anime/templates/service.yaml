apiVersion: v1
kind: Service
metadata:
  name: {{ include "sakuna-anime.fullname" . }}
  labels:
    {{- include "sakuna-anime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sakuna-anime.selectorLabels" . | nindent 4 }}
