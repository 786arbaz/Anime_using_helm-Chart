apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sakuna-anime.fullname" . }}-test-connection"
  labels:
    {{- include "sakuna-anime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sakuna-anime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
